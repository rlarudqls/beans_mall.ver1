<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="com.beans_mall.DAO.kkbOrderDAO">

	<!-- 주문 상품 정보 -->
	<select id="getGoodsInfo" resultType="com.beans_mall.VO.OrderPageItemDTO">
	
		select beanId, beanName, beanPrice, beanDiscount
		from kkb_bean where beanId = #{beanId}	
	
	</select>

	<select id="getOrderInfo" resultType="com.beans_mall.VO.OrderItemDTO">
	
		select beanId, beanPrice, beanDiscount
		from kkb_bean where beanId = #{beanId}
	
	</select>	
	
	<!-- 주문 테이블 등록 -->
	<insert id="enrollOrder">
	
		insert into kkb_order(orderId, addressee, memberId, memberAddr1, memberAddr2, memberAddr3, orderState, deliveryCost, usePoint)
    	values(#{orderId},#{addressee}, #{memberId}, #{memberAddr1}, #{memberAddr2}, #{memberAddr3}, '배송준비', #{deliveryCost}, #{usePoint})
	
	</insert>	
	
	<!-- 주문 아이템 테이블 등록 -->
	<insert id="enrollOrderItem">
	
		insert into kkb_orderItem(orderId, beanId, beanCount, beanPrice, beanDiscount, savePoint)
		values(#{orderId}, #{beanId}, #{beanCount}, #{beanPrice}, #{beanDiscount}, #{savePoint})
	
	</insert>	
	
	<!-- 주문 금액 차감 -->
	<update id="deductMoney">
	
		update kkb_member set money = #{money}, point = #{point} where memberid = #{memberId}
		
	</update>	
	
	<!-- 주문 재고 차감 -->
	<update id="deductStock">
	
		update kkb_bean set beanStock = #{beanStock} where beanId = #{beanId}
	
	</update>
	
	<!-- 주문취소 -->
	<update id="orderCancle">
	
		update kkb_order set orderState = '주문취소' where orderId = #{orderId}
		
	</update>	

	<!-- 주문 상품 정보(주문취소) -->
	<select id="getOrderItemInfo" resultType="com.beans_mall.VO.OrderItemDTO">
	
		select * from kkb_orderItem
		where orderId = #{orderId}
	
	</select>
	
	<!-- 주문 정보(주문 취소) -->
	<select id="getOrder" resultType="com.beans_mall.VO.OrderDTO">
	
		select * from kkb_order
		where orderId = #{orderId}
	
	</select>	

  </mapper>
